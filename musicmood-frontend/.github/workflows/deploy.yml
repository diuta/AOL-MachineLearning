name: Build and Deploy React App

on:
  push:
    branches:
      - main # Or your default branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18' # Specify your desired Node.js version

      - name: Install dependencies
        run: npm install
        working-directory: ./musicmood-frontend # Adjust if your package.json is in a different location

      - name: Build
        run: npm run build
        working-directory: ./musicmood-frontend # Adjust if your package.json is in a different location

      # Add deployment steps here, for example to GitHub Pages
      # - name: Deploy to GitHub Pages
      #   uses: peaceiris/actions-gh-pages@v3
      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     publish_dir: ./musicmood-frontend/build # Or your build output directory 